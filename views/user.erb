<div class="user">
  <h1><%= @user.username %></h1>
  <img src="<%= @user.avatar_img_url %>" alt="avatar">
  <p>Location: <%= @user.location %></p>
  <p>Recipes:</p>
  <ul>
    <% @user_articles.each do |article| %>
    <li><a href="articles/<%= article.id %>"> <%= article.title %> </a></li>
    <% end %>
  </ul>
</div>
<a href="/users">Back to all Chefs</a>
